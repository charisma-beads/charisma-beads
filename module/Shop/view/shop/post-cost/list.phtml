<?php $this->headTitle('Post Costs') ?>
<?php echo $this->ztbFlashMessenger()->render(); ?>

<?php if (count($this->postCosts) > 0): ?>

<table id="postCost-list" class="table table-hover table-bordered table-condensed sortable-table">
	<thead>
		<tr>
			<th data-field="postCostId">Post Level Id</th>
			<th data-field="cost">Cost</th>
			<th data-field="postLevel">Post Level</th>
			<th data-field="zone">Post Zone</th>
			<th>Actions</th>
		</tr>
	</thead>
	
	<tbody>
		<?php foreach($this->postCosts as $row): ?>
		<tr>
			<td><?=$this->escapeHtml($row->getPostCostId()); ?></td>
			<td><?=$this->escapeHtml($row->getCost()); ?></td>
			<td><?=$this->escapeHtml($row->getPostLevel()); ?></td>
			<td><?=$this->escapeHtml($row->getZone()); ?></td>
			<td>
                <a href="<?=$this->url('admin/shop/post/cost/edit', array(
                    'id' => $row->getPostCostId()
				)); ?>" class="btn">Edit</a>
				&nbsp;
				
				<!-- Button to trigger modal -->
                <a href="#postCostModel<?=$row->getPostCostId(); ?>" class="btn btn-danger" data-toggle="modal">Delete</a>
                <!-- Modal -->
				<div class="modal hide fade" id="postCostModel<?=$row->getPostCostId(); ?>">
				  <div class="modal-header">
				    <button type="button" class="close" data-dismiss="modal">&times;</button>
				    <h3 id="postCostModel<?=$row->getPostCostId(); ?>Label">Warning</h3>
				  </div>
				  <form class="form-horizontal"  method="post" action="<?=$this->url('admin/shop/post/cost/edit', array(
				        'action' => 'delete',
				    )) ?>">
				        <div class="modal-body">
				            <p>Are you sure you want to delete this post zone.</p>
				            <p class="label label-important">This action cannot be undone</p>
				        </div>
				        <input type="hidden" name="postCostId" value="<?=$row->getPostCostId() ?>">
				        <div class="modal-footer">
				            <button type="button" class="btn btn-primary" data-dismiss="modal">No Don't Do It</button>
				            <button type="submit" name="submit" class="btn btn-danger" value="delete">Yes Delete It</button>
				        </div>
				    </form>
				</div>
			</td>
		</tr>
		<?php endforeach; ?>
	</tbody>
</table>

<?=$this->paginationControl(
    $this->postCosts,
    'Elastic',
    'paginate',
	array('route' => 'admin/shop/post/cost/page')
); ?>

<?php else: ?>
<h4>No Post Costs where returned by search.</h4>
<?php endif; ?>
