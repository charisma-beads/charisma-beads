<h2>Shopping Cart</h2>
<form>
    <table class="table table-striped">
    	<tr>
    		<th>product</th>
    		<th>Description</th>
    		<th>Price</th>
    		<th>Quantity</th>
    		<th>Total</th>
    		<th>&nbsp;</th>
    	</tr>
    	<?php foreach ($this->cart()->getItems() as $item): ?>
    	<tr>
    		<td><?=$item->name;?></td>
    		<td><?=$item->category;?> / <?=$item->description;?></td>
    		<td><?=$this->cart()->formatAmount($item->price);?></td>
    		<td>
    			<input type="number" name="quantity[<?=$item->productId;?>]" class="input-mini" value="<?=$item->qty;?>">
    		</td>
    		<td><?=$this->cart()->formatAmount($item->getLineCost());?></td>
    		<td><?=$this->ztbIcon('remove');?></td>
    	</tr>
    	
    	<?php endforeach; ?>
    	
    </table>
    
    <button name="update" class="btn" value="update">Update Cart</button>
    <button name="checkout" class="btn" value="checkout">Check Out</button>
    <button name="empty" class="btn" value="empty">Empty Cart</button>
</form>