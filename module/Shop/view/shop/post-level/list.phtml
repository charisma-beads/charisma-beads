<?php $this->headTitle('Post Levels') ?>
<?php echo $this->ztbFlashMessenger()->render(); ?>

<?php if (count($this->postLevels) > 0): ?>

<table id="postLevel-list" class="table table-hover table-bordered table-condensed sortable-table">
	<thead>
		<tr>
			<th data-field="postLevelId">Post Level Id</th>
			<th data-field="postLevel">Post Level</th>
			<th>Actions</th>
		</tr>
	</thead>
	
	<tbody>
		<?php foreach($this->postLevels as $row): ?>
		<tr>
			<td><?=$this->escapeHtml($row->getPostLevelId()); ?></td>
			<td><?=$this->escapeHtml($row->getPostLevel()); ?></td>
			<td>
                <a href="<?=$this->url('admin/shop/post/level/edit', array(
                    'id' => $row->getPostLevelId()
				)); ?>" class="btn">Edit</a>
				&nbsp;
				
				<!-- Button to trigger modal -->
                <a href="#postLevelModel<?=$row->getPostLevelId(); ?>" class="btn btn-danger" data-toggle="modal">Delete</a>
                <!-- Modal -->
				<div class="modal hide fade" id="postLevelModel<?=$row->getPostLevelId(); ?>">
				  <div class="modal-header">
				    <button type="button" class="close" data-dismiss="modal">&times;</button>
				    <h3 id="postLevelModel<?=$row->getPostLevelId(); ?>Label">Warning</h3>
				  </div>
				  <form class="form-horizontal"  method="post" action="<?=$this->url('admin/shop/post/level/edit', array(
				        'action' => 'delete',
				    )) ?>">
				        <div class="modal-body">
				            <p>Are you sure you want to delete this post zone.</p>
				            <p class="label label-important">This action cannot be undone</p>
				        </div>
				        <input type="hidden" name="postLevelId" value="<?=$row->getPostLevelId() ?>">
				        <div class="modal-footer">
				            <button type="button" class="btn btn-primary" data-dismiss="modal">No Don't Do It</button>
				            <button type="submit" name="submit" class="btn btn-danger" value="delete">Yes Delete It</button>
				        </div>
				    </form>
				</div>
			</td>
		</tr>
		<?php endforeach; ?>
	</tbody>
</table>

<?=$this->paginationControl(
    $this->postLevels,
    'Elastic',
    'paginate',
	array('route' => 'admin/shop/post/level/page')
); ?>

<?php else: ?>
<h4>No Post Levels where returned by search.</h4>
<?php endif; ?>
