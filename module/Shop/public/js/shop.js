

$(document).ready(function() {

    (function(){
        var el = $('input[name=price]');

        /*if (!el.length || el.val() === '') return;

        el.attr('type', 'text');

        if (el.val().indexOf('.') === -1) {
            el.val(el.value + '.00');
        }

        while (el.val().indexOf('.') > el.val().length - 3) {
            el.val(el.val() + '0');
        }*/
    }) ();

});

